# Third-party notices

이 저장소의 원본 자료에 관한 [권리 및 재사용 조건](RIGHTS.md)은 제3자 소프트웨어나 자료의 권리를 대체하지 않는다.

## 소프트웨어 패키지

각 앱이 직접 사용하는 패키지는 해당 `package.json`에, 설치된 버전과 전이 의존성의 라이선스 메타데이터는 해당 `package-lock.json`에 기록되어 있다. 각 패키지는 해당 권리자가 정한 라이선스를 따른다.

`package-lock.json`의 라이선스 필드는 의존성 목록을 확인하기 위한 자료이며, 각 패키지의 원문 라이선스나 필수 고지를 대체하지 않는다. 각 Vite 앱의 `npm run build`는 번들에 포함된 제3자 소프트웨어의 고지를 `THIRD_PARTY_LICENSES.md`로 생성한다. 빌드·배포 산출물에는 해당 고지를 함께 보존한다.

## 초기 create-vite 템플릿

첫 공개 안전 기준선 커밋에는 이후 삭제된 `create-vite`의 TypeScript 기본
템플릿 파일과 자산 일부가 포함되어 있다. 여기에는 기본 카운터 코드, Vite·
TypeScript 로고, 시작 화면 이미지와 아이콘이 포함된다.

Vite 프로젝트의 공식 [`create-vite` 라이선스](https://github.com/vitejs/vite/blob/main/packages/create-vite/LICENSE)에
따르면 `template-*` 디렉터리의 파일과 그 파일에서 생성된 결과물에는
CC0 1.0 Universal이 적용된다. 이 예외는 해당 템플릿 유래 파일에만 적용되며,
이 저장소의 원본 코드나 창작 콘텐츠에 재사용 권한을 부여하지 않는다.

## 미디어와 창작 자산

현재 기본 브랜치에는 외부 음원, 가사, 앨범 아트, 웹폰트 파일 또는 출처가
불명확한 참고 자산을 포함하지 않는다. 이후 제3자 자산을 추가하려면 출처,
제작자, 라이선스, 수정 여부와 필요한 표기를 먼저 기록한다.
