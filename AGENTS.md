# AGENTS.md

이 문서는 사람과 코딩 에이전트가 공개 저장소 경계를 지키면서 작업하기 위한 공통 규칙이다.

## 작업 시작 순서

1. 루트 `README.md`와 `docs/project/` 문서를 읽는다.
2. 담당 게임의 `games/<slug>/README.md`와 `docs/`를 읽는다.
3. 구현은 해당 게임의 `web/` 또는 `site/` 안에서 독립적으로 수행한다.
4. 변경 뒤 담당 패키지에서 검증 명령을 실행하고 결과를 해당 게임 문서에 기록한다.

## 기록의 권위 구분

문서에서는 아래 범주를 섞지 않는다.

- **직접 요구:** 사용자가 직접 말한 목표와 제약
- **사용자 결정:** 여러 선택지 중 사용자가 채택한 내용
- **구현 사실:** 현재 파일과 실행 결과로 확인한 내용
- **제안:** 아직 채택되지 않은 개선안
- **미결정:** 추가 선택이 필요한 항목

제안이나 기존 AI 대화의 결론을 사용자 결정으로 승격하지 않는다.

## 공개·비공개 경계

- `private/`의 실제 내용은 로컬 전용이며 Git 추적 대상이 아니다.
- 공개 문서에서는 원본 파일명, 실제 로컬 경로, 공개 승인받지 않은 실명, 연락처, 작업 식별자, 비공개 인용문을 쓰지 않는다.
- 공개 승인된 이름은 채택된 게임 표현과 `CREDITS.md`의 역할 표기에만 사용하며, 이메일·성·프로필 링크 등 추가 식별 정보는 별도 결정 없이 넣지 않는다.
- 원본 근거가 필요하면 `docs/project/SOURCE_REGISTER.md`의 `SRC-*` 식별자만 사용한다.
- 비공개 원본을 코드, 테스트 픽스처, 스크린샷, 커밋 메시지에 복사하지 않는다.
- 환경 변수, 인증 정보, 배포 식별자, 로컬 설정 파일을 커밋하지 않는다.
- `RIGHTS.md`에 따라 원본 코드와 창작물에는 재사용 허가가 없다. 제3자 자료에는 각 자료의 라이선스를 보존한다.
- 공개 전 `git status`, `git diff --cached`, `git ls-files`를 확인한다.

## 경로와 의존성

- 정식 경로는 `games/<slug>/{web,site?,docs}`이다.
- 예전 최상위 코드 경로는 로컬 호환 별칭일 수 있다. 별칭을 편집하거나 공개 문서에서 새 링크로 사용하지 않는다.
- 로컬 별칭은 Git에 추적되지 않으므로 새 clone에서는 존재하지 않는다. 이전 경로는 `docs/project/REPOSITORY_MAP.md`로 변환한다.
- 게임끼리 소스 파일이나 런타임 상태를 직접 참조하지 않는다. 공용 패키지는 별도 사용자 결정 전까지 만들지 않는다.
- 중첩 Git 저장소를 새로 만들지 않는다.

## 현재 범위

활성 작업 범위는 다음 다섯 slug뿐이다.

- `man-in-word`
- `sister-wakeup`
- `forever-young`
- `breakup-notice`
- `night-flight`

현재 범위 밖의 후보를 위해 폴더, 문서, 작업을 선제적으로 만들지 않는다.

## 변경과 배포

- 제품 동작을 바꾸면 관련 게임 문서의 직접 요구, 구현 사실, 검증 결과를 함께 갱신한다.
- 대규모 구조 변경은 먼저 `DECISION_LOG.md`에 채택 여부를 기록한다.
- 빌드 산출물은 재생성 가능하면 추적하지 않는다. 배포에 실제로 필요한 정적 자산은 출처와 갱신 방법을 문서화한다.
- 외부 공개, 원격 저장소 생성·push, 배포, 라이선스 추가, 공개 크레딧 표기는 명시적인 사용자 결정 없이 수행하지 않는다.
